import { Routes } from '@angular/router';
import { ImageCompressorComponent } from './image-compressor/image-compressor.component';

export const routes: Routes = [
    // {path: '', component: , title: 'Image-Compressor'},
    // {path: 'image-compressor', component: ImageCompressorComponent},
    // {path: '', redirectTo: 'image-compressor'}
];
